to run on localhost:

- (package manager) install node
- (package manager) install npm
- npm install node-static
- node run.node.js
- navigate to localhost:8080


if we go all coffeescript with this, we will want to convert the stuff we already have in JS to it with js2coffee.org